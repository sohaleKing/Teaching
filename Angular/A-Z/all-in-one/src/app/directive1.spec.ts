import { Directive1 } from './directive1';

describe('Directive1', () => {
  it('should create an instance', () => {
    const directive = new Directive1();
    expect(directive).toBeTruthy();
  });
});
